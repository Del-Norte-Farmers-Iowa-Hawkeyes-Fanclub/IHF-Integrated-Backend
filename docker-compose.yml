version: '3.8'

services:
  spring:
    build:
      context: ./spring
    volumes:
      - ./sqlite.db:/app/sqlite.db
    ports:
      - "6942:6942"

  flask:
    build:
      context: ./flask
    volumes:
      - ./sqlite.db:/app/sqlite.db
    ports:
      - "8086:8086"